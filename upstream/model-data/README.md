# Pinned Pi Base model data

`pi-ai-0.81.1/` is the generated model-data snapshot extracted from the official npm package `@earendil-works/pi-ai@0.81.1`.

The package reported npm integrity:

`sha512-hzHE7Z8l5mgJk+ke67Lge0rwS2+wbKJrFKl9o5M1R1rh33+cCT7D1AHz1OAtX5wFs90E1/BTGhyJRTUHaMxGvQ==`

Its model-data manifest SHA-256 is:

`4bbfc496835ea5e011436124042601259d8fd8ba37edca4a1fe1927244a5ab23`

The Pi Base lock binds both values. Installation verifies the manifest, its complete file list, and every file digest before copying the snapshot into the exact Pi Base checkout. The built-in Pi validation then checks the data against the pinned source's structural model catalog.

This records reproducible build input; it is not an additional publisher-authentication claim.
